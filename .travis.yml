language: ruby
bundler_args: --without debug
script: "bundle exec rspec spec"
sudo: false
cache: bundler
rvm:
 - 2.2
 - 2.3
 - 2.4
 - ruby-head
 - jruby
 - rbx
matrix:
  allow_failures:
      - rvm: jruby
      - rvm: rbx
      - rvm: ruby-head
