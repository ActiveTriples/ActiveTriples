language: ruby
bundler_args: --without debug
script: "bundle exec rspec spec"
sudo: false
cache: bundler
rvm:
  - 2.1.10
  - 2.2.5
  - 2.3.1
  - ruby-head
  - jruby
  - rbx-2
matrix:
  allow_failures:
      - rvm: jruby
