language: ruby
bundler_args: --without debug
script: "bundle exec rspec spec"
sudo: false
cache: bundler
rvm:
  - 2.1.8
  - 2.2.4
  - 2.3.0
  - ruby-head
  - jruby
  - rbx-2
matrix:
  allow_failures:
      - rvm: jruby
      - rvm: ruby-head
